While you already know this, here is a reminder of the Pact-JS classes and methods you will need to use to create a Pact test in JavaScript (having omitted deprecated and unadvised methods):

> **Auto-generated** from pact-js source by `scripts/generate/dsl_js.py` in pact-agent-skills. Do not edit this file directly — run the workflow or the script instead.

## V3 API

File: src/v3/pact.ts
```javascript
// const { PactV3 } = require("@pact-foundation/pact");
class PactV3 {
    constructor(opts);
    addInteraction(interaction);
    given(providerState, parameters);
    uponReceiving(description);
    withRequest(req);
    withRequestBinaryFile(req, contentType, file);
    withRequestMatchingRules(req, rules);
    withResponseMatchingRules(req, rules);
    withRequestMultipartFileUpload(req, contentType, file, mimePartName, boundary);
    willRespondWith(res);
    withResponseBinaryFile(res, contentType, file);
    withResponseMultipartFileUpload(res, contentType, file, mimePartName, boundary);
    executeTest(testFn);
}
```

File: src/v3/matchers.ts
```javascript
// const { MatchersV3 } = require("@pact-foundation/pact");
namespace MatchersV3 {
    function like(template);
    function eachKeyMatches(example, matchers);
    function eachValueMatches(example, matchers);
    function eachLike(template, min);
    function atLeastOneLike(template, count);
    function atLeastLike(template, min, count);
    function atMostLike(template, max, count);
    function constrainedArrayLike(template, min, max, count);
    function boolean(b);
    function integer(int);
    function decimal(num);
    function number(num);
    function string(str);
    function regex(pattern, str);
    function contentType(contentTypeValue);
    function equal(value);
    function datetime(format, example);
    function timestamp(format, example);
    function time(format, example);
    function date(format, example);
    function includes(value);
    function nullValue();
    function url2(basePath, pathFragments);
    function url(pathFragments);
    function arrayContaining(...variants);
    function fromProviderState(expression, exampleValue);
    function uuid(example);
    function reify(input);
}
```

File: src/xml/xmlBuilder.ts
```javascript
class XmlBuilder {
    constructor(_version, _charset, rootElement);
    build(callback);
}
```

File: src/xml/xmlElement.ts
```javascript
class XmlElement {
    constructor(name);
    setName(name);
    setAttributes(attributes);
    appendElement(name, attributes, arg);
    appendText(content);
    eachLike(name, attributes, cb, options);
}
```

## V4 API

File: src/v4/index.ts
```javascript
// const { PactV4 } = require("@pact-foundation/pact");
class PactV4 {
    constructor(opts);
    addInteraction();
    addSynchronousInteraction(description);
    addAsynchronousInteraction();
    addGraphQLInteraction();
}
```

File: src/v4/http/types.ts
```javascript
// V4 HTTP interaction builder chain:
// pact.addInteraction()
//   .given(state, params)          // optional, repeatable
//   .uponReceiving(description)
//   .withRequest(method, path, (b) => { b.query(...); b.jsonBody(...); })
//   .willRespondWith(status, (b) => { b.headers(...); b.jsonBody(...); })
//   .executeTest(async (mockserver) => { ... });
//
// Async message chain:
// pact.addAsynchronousInteraction()
//   .given(state)
//   .expectsToReceive(description, (b) => { b.withJSONContent(...); })
//   .executeTest(async (m) => { ... });

// V4UnconfiguredInteraction:
{
    given(state, parameters);
    uponReceiving(description);
    pending(pending);
    comment(comment);
    testName(name);
    reference(group, name, value);
    withCompleteRequest(request);
    withRequest(method, path, builder);
    usingPlugin(config);
}

// V4RequestBuilder:
{
    query(query);
    headers(headers);
    jsonBody(body);
    xmlBody(body);
    binaryFile(contentType, file);
    multipartBody(contentType, filename, mimePartName, boundary);
    matchingRules(rules);
    body(contentType, body);
}

// V4ResponseBuilder:
{
    headers(headers);
    jsonBody(body);
    xmlBody(body);
    binaryFile(contentType, file);
    multipartBody(contentType, filename, mimePartName, boundary);
    matchingRules(rules);
    body(contentType, body);
}
```

File: src/v4/message/types.ts
```javascript
// V4UnconfiguredAsynchronousMessage:
{
    given(state, parameters);
    pending(pending);
    comment(comment);
    testName(name);
    reference(group, name, value);
    usingPlugin(config);
    expectsToReceive(description, contents);
}

// V4AsynchronousMessageBuilder:
{
    withMetadata(metadata);
    withContent(contentType, body);
    withJSONContent(content);
    withMatchingRules(rules);
}
```

## V2 (Legacy) DSL

File: src/dsl/interaction.ts
```javascript
class Interaction {
    given(providerState);
    uponReceiving(description);
    withRequest(requestOpts);
    willRespondWith(responseOpts);
    json();
}
```

File: src/dsl/verifier/verifier.ts
```javascript
// const { Verifier } = require("@pact-foundation/pact");
class Verifier {
    constructor(config);
    verifyProvider();
}
```

---

## V3 Consumer Test (Javascript)

> Source: [`examples/v3/e2e/test/consumer.spec.js`](examples/v3/e2e/test/consumer.spec.js)

```javascript

```

---

## V4 Consumer Test (TypeScript)

> Source: [`examples/v4/typescript/test/get-dog.spec.ts`](examples/v4/typescript/test/get-dog.spec.ts)

```typescript

```

---

## Provider Verification

> Source: [`examples/v3/e2e/test/provider.spec.js`](examples/v3/e2e/test/provider.spec.js)

```javascript

```
